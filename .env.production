# Production Environment Variables Template

## Frontend (.env.local)
# Copy this configuration when deploying to production

# Production Backend URL (replace with your actual Render backend URL)
NEXT_PUBLIC_WS_URL=https://online-xox-backend.onrender.com

## Backend (Render Environment Variables)
# Set these in your Render backend service dashboard

NODE_ENV=production
PORT=10000
CORS_ORIGIN=https://online-xox-frontend.onrender.com

## Notes:
# - Replace the URLs with your actual Render service URLs
# - Backend PORT=10000 is Render's default
# - CORS_ORIGIN should match your frontend URL exactly
# - These variables are set in Render dashboard, not in files